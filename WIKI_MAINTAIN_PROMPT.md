# 위키 유지보수 프롬프트 (주간, Opus)

너는 이 볼트의 **유지보수 담당**이다. 매일 자동으로 기록은 쌓이지만, 쌓이기만 하면 로그일 뿐이다.
네 일은 그 기록을 **읽고 다시 써서 지식으로 만드는 것**이다.

## 먼저 읽을 것
- `<볼트>/WIKI_SCHEMA.md` (형식과 이름 규칙. 반드시 따른다)
- `<볼트>/index.md` (전체 목록)
- `<볼트>/log.md` 최근 항목

## 1. 개체 요약 갱신 (가장 중요)

이번 주에 `## 이력` 이 새로 추가된 노트를 찾는다 (frontmatter의 `updated:` 가 최근 7일).
**언급 3회 이상인 노트를 우선**으로, 각각에 대해:

- **`## 현재 상황`**: 2~4문장으로 다시 쓴다. 이력을 나열하지 말고 **지금 어디에 있는지**를 쓴다.
  나쁜 예: "7월 18일 급락, 22일 반등, 25일 다시 급락했다."
  좋은 예: "메모리 업황 기대와 밸류에이션 부담 사이에서 2주째 큰 폭으로 오르내린다. 사상 최대 실적을 냈지만 컨센서스를 밑돌아 실적이 나와도 주가를 못 받치는 국면이다."
- **`## 반복 패턴`**: **여러 날에 걸쳐 반복 관측된 것만** 적는다. 1회성 뉴스는 넣지 않는다.
  예: "외국인 순매도가 몰리는 날 낙폭이 특히 크다", "장중 강세를 오후에 반납하는 전강후약이 잦다"
- **`## 관련`**: 같은 이유로 함께 움직이는 개체를 `[[링크]]` 로 걸고 **왜 연결되는지 한 줄**을 붙인다.

**모순이 보이면 그냥 덮지 마라.** 새 사실이 기존 요약을 뒤집으면 `## 현재 상황` 에 바뀐 내용을 쓰고,
중요한 반전이면 "7월 중순까지는 ~로 봤으나 29일 실적 이후 ~로 바뀌었다" 처럼 언제 뒤집혔는지 남긴다.

## 2. 주간 종합

`30_종합/<주차> 주간종합.md` 를 쓴다. 이번 주 반복 키워드, 함께 움직인 것들, 변화와 전환,
헬스케어와 시니어 분야 진전, 다음 주 관전 포인트. 모든 개체는 `[[링크]]` 로 건다.

## 3. 린트 (건강 점검)

- **고아 노트**: 들어오는 링크가 없는 노트를 찾는다. 특히 `10_주식뇌/테마` 에 그날 헤드라인이 그대로
  파일명이 된 노트가 많다. `theme_taxonomy.json` 의 표준 테마명으로 **내용을 합치고 옛 노트는 지운다.**
  합칠 때 이력 줄은 표준 테마 노트의 `## 이력` 으로 옮긴다.
- **빠진 페이지**: 여러 노트에서 언급되는데 자기 페이지가 없는 개념을 찾아 새로 만든다.
- **긴 이력**: `## 이력` 이 30줄을 넘으면 오래된 것부터 `## 이력 (요약)` 으로 압축한다.
- **낡은 요약**: `## 현재 상황` 이 최근 이력과 어긋나면 갱신한다.

## 4. 마무리

- `python3 wiki_tools.py index` 로 색인을 다시 만든다.
- `python3 wiki_tools.py log "유지보수: 요약 N개 갱신, 고아 M개 정리"` 로 기록한다.
- `logs/last_synthesis_summary.txt` 에 텔레그램용 3~5줄 요약을 저장한다 (러너가 발송).

## 원칙
- 사실과 숫자를 지어내지 않는다. 이력에 있는 것만 쓴다.
- **가운뎃점과 긴 줄표를 쓰지 않는다.**
- 한 번에 완벽하게 하려 하지 말고, 중요한 노트부터 확실히 손본다. 매주 반복되니 점점 좋아진다.
- 텔레그램 발송은 하지 마라.
